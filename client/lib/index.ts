import {init} from './render';
import {initStore, addReducer, dispatchEvent, getStore, UPDATE_EVENT} from './store';

export {
    init as render,
    initStore,
    addReducer,
    dispatchEvent,
    getStore,
    UPDATE_EVENT
}
